import React, { Fragment } from 'react';
import Layout from '../components/navigation/defaultLayout';

const CV = () => (
  <Fragment>
    <Layout>
    CV hi
    </Layout>
  </Fragment>
);

export default CV;
